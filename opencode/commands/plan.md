---
description: Phase 2 of EPCV — turn approved exploration findings into an atomic, testable implementation plan
agent: planner
---

# Plan

Plan this work:

$ARGUMENTS

Follow the `writing-plans` skill. Requires approved exploration findings — if none exist in context or Backlog, stop and recommend `/explore` first.
