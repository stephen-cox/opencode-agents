---
description: Phase 3 of EPCV — implement one approved atomic task
agent: coder
---

# Code

Implement this atomic task:

$ARGUMENTS

Follow the `implementing-tasks` skill. Requires an approved plan with a task brief — if none exists in context or Backlog, stop and recommend `/plan` first. After implementation, run `/verify`, then `/commit-task` to commit.
